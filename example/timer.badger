var x = 0

let timer = createPeriodicTimer(1000, () -> {
  x = x + 1

  if x == 5 {
    cancelTimer(timer)
    createTimer(2000, () -> {
      print("This will run after the timer.")
    })
    return
  }

  print(x)
})

print("This is still going to run before the timer.")
